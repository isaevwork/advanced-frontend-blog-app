import React, { useContext } from 'react';

const Sidebar = () => {

    return (
        <aside>
            {/*<button onClick={() => setTheme(theme)}>Toggle</button>*/}
        </aside>
    );
};

export default Sidebar;